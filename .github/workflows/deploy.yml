name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Restore config.local.js from secret
        run: |
          echo "${{ secrets.BOWIE_CONFIG_LOCAL }}" > docs/config.local.js
      
      - name: Create debug.local.js for testing
        run: |
          echo "${{ secrets.DEBUG_LOCAL_JS }}" > docs/debug.local.js
          echo "✅ Created debug.local.js"
          ls -la docs/debug.local.js
          echo "📄 Content preview:"
          head -n 3 docs/debug.local.js
      
      - name: Create config.secret.js for testing (NOT used by main app yet)
        run: |
          echo "${{ secrets.CONFIG_SECRET_JS }}" > docs/config.secret.js
          echo "✅ Created config.secret.js for testing"
          ls -la docs/config.secret.js
          if [ ! -s docs/config.secret.js ]; then
            echo "❌ config.secret.js is empty!"
            exit 1
          fi
          echo "📄 Content preview:"
          head -n 3 docs/config.secret.js

      - name: Cache-bust static assets
        run: |
          VERSION=${GITHUB_RUN_NUMBER:-$GITHUB_SHA}
          perl -pi -e 's|styles/styles\.css(\?v=[^"'"'"'">]+)?|styles/styles.css?v='"${VERSION}"'|g' docs/index.html
          perl -pi -e 's|config\.js(\?v=[^"'"'"'">]+)?|config.js?v='"${VERSION}"'|g' docs/index.html
          perl -pi -e 's|app\.js(\?v=[^"'"'"'">]+)?|app.js?v='"${VERSION}"'|g' docs/index.html
      
      - name: Setup Pages
        uses: actions/configure-pages@v4
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: 'docs'
      
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
