# ts-wg-bridges.conf
# Subnets to bridge from Tailscale → WireGuard at boot.
# One CIDR per line. Lines starting with # are comments.
# Managed by: ts-wg-bridge  (add / --remove)
# Restored by: event.d/boot/55-ts-wg-bridge-restore.sh

10.253.0.0/24
